<h1><%=t 'heading.editing' %> <%=t 'items' %></h1>

<%= render :partial => 'form', :locals => {:submit => I18n.t('submit.update')} %>

<%= link_to I18n.t('link.back'), {:controller => :objective, :action => :maintenance,
	:id => @root_division} %>
